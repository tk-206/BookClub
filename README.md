북클럽
책과 사람이 만나는 공간.
독자, 작가, 출판사 모두를 위한
문학 커뮤니티 플랫폼.

당장 해야하는 것들
1. 로그인 토큰 기능 정리 + 구현 완료 하기
2. 미구현된 화면 구현하기
3. 폼 검증 확인하기
4. 공통 컴포넌트 화 시키기 (버튼, 모달, 카드, 탭 등등)
ex) 페이지 컴포넌트: 화면 전체의 큰 흐름과 데이터 연결 담당
    섹션 컴포넌트: 헤더, 사이드바, 리스트, 카드 영역처럼 의미 있는 화면 덩어리 담당
    공통 UI 컴포넌트: Button, Modal, Card, EmptyState, Pagination처럼 여러 곳에서 재사용되는 요소 담당
    hooks: 필터링, 모달 열림/닫힘, 데이터 요청 같은 상태 로직 담당
    types/api: 데이터 타입과 서버 통신 로직 담당

----------
적용 or 고려 해봐야 하는 것
1. 반응형 레이아웃 / 모바일 우선 UI
2. 주요 사용자, 사용층 구분해서 명확하게 사용 구조 구분하기
3. 폰트 균일화 시키기
4. 

----------
개념 정리해야할 것들.
1. html/css
2. react
3. 데이터 캐싱 전략
4. 토큰 처리
5. 렌더링 구조
6. Fast Refresh 규칙
7. lint 규칙 (+ 역할 분리)

---------------------
GPT가 준 목표.
북클럽 리뉴얼 + 배포
로그인
반응형
검색
무한스크롤 or pagination
에러 처리
skeleton UI
SEO 조금
README 제대로
기술 깊이 추가
TS 제대로
React Query
Zustand/Jotai 같은 상태관리
테스트 조금
성능 개선 경험
팀프로젝트 1개
여기서 협업 경험 꼭 얻어야 함.
하루 최소 4~6시간은 “실제 구현”
강의 중독 절대 금지.

질문 사항
1. 데이터를 먼저 불러오고 화면을 구성하는가? or 화면 상황 발생 시 데이터를 불러오는가?
2. 웹에서는 데이터를 어떤 식으로 저장하고 가공하고 할까?


메뉴, 탭 컴포넌트 화 (기억하기)
type Tab = '게시글' | '독서 토론' | '작가 채널'

const [tab, setTab] = useState<Tab>('게시글')

const TAB_COMPONENT = {
  게시글: PostList,
  '독서 토론': Discussion,
  '작가 채널': AuthorChannel,
}

let PostList
if(true) {
  PostList = (
    <div></div>
  )
}

or

컴포넌트 변수 등록

- 맥 단축키
쉬프트 + 알트 + a = <!--  -->(주석)
알트 + 백스페이스 = 단어 지우기
커맨드(윈도우) + 엔터 = 줄 안건드리고 넘어가기
쉬프트 + 스페이스 (변경된 내 단축키) = 자동 완성

-------------------------------------------
오늘 할 일

1. modal만들어서 연결하기. (구인구직)
